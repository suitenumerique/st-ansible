{# keycloak env — KC_DB, features, metrics, health, HTTP and proxy settings are baked into the image #}
KC_DB_URL={{ answers.KC_DB_URL }}
KC_DB_USERNAME={{ answers.KC_DB_USERNAME }}
KC_DB_PASSWORD={{ answers.KC_DB_PASSWORD }}
KC_HOSTNAME={{ answers.KC_HOSTNAME }}
KC_BOOTSTRAP_ADMIN_USERNAME={{ answers.KC_BOOTSTRAP_ADMIN_USERNAME }}
KC_BOOTSTRAP_ADMIN_PASSWORD={{ answers.KC_BOOTSTRAP_ADMIN_PASSWORD }}
